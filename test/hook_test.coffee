# -*- coding:utf-8; mode:coffee; -*-

describe 'On hook.coffee, ', ->

  describe '#.isIgnoreEvent', ->

    event = null

    describe 'with non backspace, no alt key and no ctrl key', ->
      it 'should return false', ->
        isIgnoreEvent event

    describe 'with non backspace, pressed alt key and no ctrl key', ->

    describe 'with backspace, no alt key and no ctrl key', ->

    describe 'with backspace, pressed alt key and no ctrl key', ->

    describe 'with backspace, no alt key and pressed ctrl key', ->


